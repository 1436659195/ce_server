# 安全策略 / Security Policy

> Coding Everywhere (CE) 是端到端加密 (E2E) 的远程开发 / 指挥工具。**安全是它的核心卖点**——
> 这份文件说清楚三件事:我们的安全模型长什么样、什么算漏洞、怎么**私下**报给我们。
>
> Report vulnerabilities **privately** via GitHub Security Advisories — please **do not** open a public issue.

---

## 🔒 先读:我们的安全承诺

CE 的信任根基只有一句:**中继 (relay) 只转发密文,无法解密你的流量。**

- **加密**:`tweetnacl`(NaCl 语义,公钥加密 / 密钥协商在两端完成)。解密**只发生在两端**——你的手机 App 和被控机上的 `ce` CLI,会话私钥不离开这两端。
- **中继**:公网转发枢纽,**按设计不可信(untrusted-by-design)**,不持有任何会话密钥、看不到明文。
- **会话 token**:`relay-state.json` 落盘,用 `RELAY_STATE_KEY` 经 KDF 派生密钥做 **AES-256-GCM** 加密、文件权限 `0600`。
- **配对**:手机扫码 / 输连接码完成,绑定两端身份。

> **如果你发现了一条让中继(或任何第三方)能解密流量、或让会话私钥离开端点的路径——这是最高优先级 (Critical) 的漏洞,请立刻按下面的方式报告。**

---

## 📨 报告漏洞

**走私下渠道,不要开 public issue / PR(避免 0day 暴露)。**

1. **首选**:GitHub → 本仓库 **Security** 标签 → **Report a vulnerability**
   生成私有 Security Advisory,全程不公开,只有维护者可见。(维护者:需在仓库 Settings → Security & analysis 里开启 *Private vulnerability reporting*。)
2. **备用邮箱**:`security@example.com` ← **(维护者:替换为你的安全邮箱;或删掉此行只留 GitHub 渠道。)**

**我们会:**
- **72 小时内**确认收到;
- 尽快评估严重性并给初步时间表(这是一个 pre-1.0 的小项目,我们会**坦诚沟通节奏**,不夸大企业级 SLA);
- 修复并发布后**公开致谢**(除非你要求匿名)。

**善意研究免责 (Safe Harbor):** 针对你**自己的账号 / 你自己部署的中继 / 你拥有的被控机**进行善意测试,我们不予追究。严禁影响他人、不可接触他人数据、不可破坏可用性。

---

## ✅ 在范围内(我们想知道的)

按严重性从高到低:

- **破坏 E2E**:任何让中继 / 第三方能解密流量、或让会话私钥离开端点的路径。
- **鉴权绕过**:伪造配对(QR / 连接码)、会话劫持、冒充被控机或手机端、中继 session 伪造。
- **`ce` CLI / 中继的 RCE**、命令注入、**路径穿越**(尤其 `ce` 侧文件 / 终端能力越权读取 `.ssh` / `.env` / 密钥等敏感路径)。
- **`curl|sh` 安装链路的完整性**:中继 `/dl/` 下发的 `ce` 二进制若可被投毒 / 替换 → 每台被控机 RCE。sha256 指纹校验、下载通道是否可被 MITM,都在关注范围内。
- **加密实现缺陷**:弱密钥派生、nonce 复用、降级攻击。
- **`relay-state.json` token 泄露**,或 `RELAY_STATE_KEY` 未设时的明文回退被远程利用。

## ⚠️ 一般不在范围内(节省彼此时间)

- **自托管中继的配置问题**:你没设 `RELAY_STATE_KEY` → token 明文落盘——这是文档化的降级行为、是你的选择,非漏洞。
- **中继可见的元数据 / IP / 连接时序 / 流量大小**:中继是转发枢纽,看得见这些是**设计如此**。CE 不保证对中继隐藏元数据(需要的话用直连或自建中继)。
- **拿到已解锁手机的物理访问**之后的攻击、**社工**。
- **DoS**:针对自托管中继的洪泛 / 资源耗尽。
- **第三方依赖里的漏洞**:请报给上游;除非 CE 的特定用法使其可被实际利用。

> 以上"不在范围"不是借口,是想让研究者把火力放在真正能破坏 E2E / 越权的地方。

---

## 🔢 支持版本

CE 目前 pre-1.0(`0.1.x`),迭代很快。

| 版本 | 安全修复 |
|---|---|
| 最新 release / `main` | ✅ 支持 |
| 旧版本 | ❌ 不回溯(pre-1.0,请升级到最新) |

---

## 📝 报告里请包含

- **复现步骤**(尽量最小化);
- **影响**:能干什么、攻击者所处的位置(中继运营者?配对过的另一端?网络中间人?匿名互联网用户?);
- **环境**:OS、`ce` / 中继版本、是否自托管中继、是否设了 `RELAY_STATE_KEY`;
- 可选:PoC、patch 建议。

---

## 🏅 致谢

感谢负责任披露的安全研究者将在此列出(除非要求匿名)。
_(暂无)_
